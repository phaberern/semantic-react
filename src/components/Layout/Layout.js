import React from 'react'

const Layout = (props) => (
    <Container>
        <Header as='h1'>Home</Header>
        <Divider />
        <ComplianceSpecialistCard />
    </Container>
)

export default Layout